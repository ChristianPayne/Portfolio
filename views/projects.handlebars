<main class="">
    <div class="grid grid-flow-row grid-cols-2 gap-10 m-5">


        {{!-- Genesis card --}}
        {{>gen-details}}

        {{!-- Hyundai card --}}
        {{>hyn-details}}
            
        {{#each projectData.projects as |project|}}
        {{>project-details}}
        {{/each}}
    </div>
</main>